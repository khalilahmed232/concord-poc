configuration:
  runtime: "concord-v2"

flows:
  default:
  - log: "Hello Concord User"
  - task : docker
    in: 
      image: python
      saveOut: true
      cmd: |
        echo "Executing python"
    out:
      dockerResult
  - log: ${dockerResult}
