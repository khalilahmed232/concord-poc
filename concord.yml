configuration:
  runtime: "concord-v2"

flows:
  default:
  - log: "Hello Concord User"
  - task : docker
    in: 
      image: python
      saveOut: true
      cmd: |
        echo "Executing python"
        chmod 777 hello.py
        python3 hello.py
        pip3 install -r requirements.txt
    out:
      dockerResult
  - log: ${dockerResult.stdout}
